# Agent 协作入口

工程约束、任务文档路由、测试和提交规则统一见 `AGENTS.md`。上下文已有当前完整规则时无需重读；架构细节按任务读取对应专题，不在本文件维护副本。

## 协作偏好

1. 用中文回复，默认在已授权范围内自主推进。
2. 仅未获授权的数据删除、破坏性契约变更、对外发布等操作需要确认；已有明确授权不重复询问。缺少决定结果的必要信息时集中提问，并继续不依赖答案的工作。自检中的“确认”不是向用户请求批准。
3. 先按入口和相关文档定位，再精准搜索；入口不足时可做有界的文件名或符号发现。
4. 多个工单或交付物批量说明依赖与可并行部分，减少来回确认。
5. 每张施工工单验收后按 `AGENTS.md` 独立提交，无需另问。只暂存本任务改动；同一文件有他人修改时按 hunk 暂存，不以文件归属代替差异核对。只读审查不产生提交。
6. Commit message 附加 `Co-authored-by: Codex <codex@openai.com>`。

## 文档同步

仅行为、契约或操作说明变化时更新相关文档。Claude Code 文档同步 hook 仅在实际配置启用时适用；不假定本地备份中的 hook 已启用。若 hook 提醒但无需文档变化，说明 `no doc update needed: <reason>`，不为通过检查制造无意义文档改动。
