# 五项后端需求施工与续接清单

日期：2026-09-11。仅在后端仓库施工；客户端只读核对并提供交接工单。
每项完成相关测试、差异检查后独立 commit，随后打勾。未完成不得勾选。

## 总进度

- [x] 1. 管理面模型连接：获取中转模型目录、直接选择、兼容手填。
- [x] 2. 模型返回思考：检查后端读取契约，补齐必要能力，提供前端工单。
- [ ] 3. IME：按真实传输字段接收三小时记录，只接数据，不接入推理或自动任务，不启动真实传输。
- [ ] 4. Gmail 周信：排查配置、调度、周契约、发送与重试链，修复可复现问题。
- [ ] 5. 小红书分享读取工具：正文、图片、评论，后端可开关，明确不可用与部分结果。

## 工单 1：模型目录

读取现有模型连接与 preset 保存链。新增管理员目录查询，支持未保存的 URL/key
以及已保存连接的 key；正确处理根地址、版本路径、完整生成端点。
目录不可用、鉴权失败、超时、异常格式和空列表都有明确提示，保留手动输入。
目录不代表模型支持某协议或工具，不自动覆盖协议/路由。管理面提供加载与选择交互。
验收：目录与原保存路径回归、缓存版本更新、浏览器刷新实测。

## 工单 2：模型思考后端与前端交接

核对实际 API 返回思考归档、分页详情、鉴权、聊天关联键与中断状态。
确定前端能可靠消费的接口；必要后端修改须测试，不将归档正文混入聊天或记忆。
交接工单写明接口、字段、权限、分页、空结果/错误/中断降级及验收。
不跨仓修改前端代码。

## 工单 3：IME 接收预备

只读相邻 IME 仓库文档与发送代码，以真实协议定义接收端，不臆造字段。
接收数据保持原始语义，提供去重、明确 ack 与只读观测；数据路径走 sandbox。
保持接收用途与 pipeline、记忆、主动消息隔离；不更改手机配置、不启动真实传输。
验收：真实形状合成 fixture、重复投递、错误输入、观测与隔离测试。

## 工单 4：Gmail 周信排障

只读核对本地运行迹象与配置（不输出凭据），检查每周到期、窗口、QUIET 闸门、
重试/租约、质量门与 SMTP 失败记录。以证据区分配置阻断与代码缺陷。
不发送真实邮件；测试 mock SMTP/LLM。记录原因、修复、剩余部署观察项。

## 工单 5：小红书工具

核对现有 URL/图片读取与工具入口，验证可用的帖子/评论获取方式。
实现显式开关与 effective state，注册 examples/keywords；解析分享文案与短链，
返回正文、图片和评论的来源及完整性，不把登录限制或失败当成空评论。
优先复用现有能力，若依赖外部服务则明确所需配置和不可用原因。
验收：开关、分享解析、成功/部分/失败结果、原工具路径与相邻路径回归。

## 共同验收与续接

每项同步专题文档、feature-control-surface（适用时）、接口总账；未覆盖客户端部分
以 open/roadmap/observe 同步 known-issues 与接口总账。只暂存本任务改动。
当前已有 cc-tasks 229–241 删除及临时工单修改，属于既有工作区改动，不纳入本任务。

## 执行记录

- 已读取工程入口、开发环境与模型专题。初步确认思考归档已有 admin-only 列表/详情，
  文档指出尚无聊天 turn_id 关联；周信已有 weekly_contract，需要继续核对实际阻断。
