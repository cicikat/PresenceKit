# 六项需求续接与施工清单

日期：2026-09-11。按项实施、测试、检查差异并独立提交；仅完成验收后勾选。
不纳入工作区已有的工单删除和临时工单修改。真实凭据仅写 ignored 本地文件。

## 工单 A：IME 配对与后端观测（需求 1、2）
- [x] 核对 IME 实际 URL 拼接、Bearer 协议、内网穿透地址和接收开关。
- [x] 为 IME 配置独立凭据，保存在本地密钥本；不轮换其他设备凭据。
- [x] 管理面合适位置展示接收状态、三小时保留、记录和错误/空状态。
- [x] 接收/鉴权/相邻 sensor 回归，缓存版本、浏览器实测、接口文档闭环、独立提交。

## 工单 B：模型 preset 诊断（需求 3）
- [x] 复现测试失败，核对中转 API 路径、协议、模型、参数与代理。
- [x] 区分网络、超时、鉴权、额度/限流、协议/路径、模型及响应格式；提供可操作提示，隐藏密钥。
- [x] 验证原测试/保存/真实调用链，管理面浏览器实测、客户端审计与独立提交。

## 工单 C：主动消息记忆及 HTML 历史（需求 4）
- [ ] 核对主动消息发送、送达、历史与记忆链；实际已发送消息进入合适历史/记忆入口并去重。
- [ ] 保持来源隔离与 assistant 脱敏，不将模型主动推测当作用户确认事实。
- [ ] HTML 呈现元数据持久化并在重启后历史恢复；核对桌面/手机消费与安全渲染。
- [ ] 原路径与相邻路径回归、文档、独立提交。

## 工单 D：生活记录（需求 5）
- [ ] 完成现有 `245-life-records-backend-handoff.md` 的 B1–B8；逐项在原单勾选。

## 工单 E：小红书本地部署（需求 6）
- [ ] 核对上游部署方式，安装本地读取服务，登录材料仅本地保存。
- [ ] 加入请求串行、间隔、限流退避、登录/风控失败停止重试及可理解错误。
- [ ] 管理面接线与真实只读验证；需用户扫码时明确提示，不采集账号密码。
- [ ] 文档、回归及独立提交；未完成登录和真站验证不得声称部署完成。

## 初步核对
- IME 已有 `POST /v1/ime/drafts`（sensor.write）与 admin-only `GET /observability/ime-drafts`；尚未核对真实配置。
- 生活记录原单仍全部待办。
- 小红书已有只读适配，文档明确上轮没有部署远端读取服务或登录。
