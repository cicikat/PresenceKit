# 00e · 全局审计：孤立/半成品/带闸门功能清单（2026-07-17，2026-07-18 更新）

> 审计方式：docs 全量标记扫描（demo/占位/未实现/未接线/默认关）+ system-readiness +
> 00b 交接 + known-issues + 代码调用方反查。分五类：孤立断线 / demo 模式 /
> 带闸门待观察（不是债）/ 未竟系列 / 文档对齐小账。
>
> **2026-07-18 更新**：A1（Brief 88）→ A2（Brief 89）→ B2（Brief 90）→ E 三处顺手
> 已全部完成并通过 `pytest -n auto`（93 passed，覆盖 hidden_state / global_facts /
> dream_mirror 相关用例）。以下条目状态已就地更新，原表结构保留供追溯。

## A. 孤立断线（有基建、无调用方或无回流）——真正的"精装修空房"

| # | 项 | 现状 | 建议 |
|---|---|---|---|
| A1 | ~~**H1：user_hidden_state 现实侧写入**~~ **已完成（Brief 88）** | `RealityEventType` 扩至 5 类（新增 `BODY_TOPIC`/`AFFECTION_EXPRESSED`）；对话侧判定落在 `core/memory/user_hidden_state_reality_signals.py`，挂 `pipeline.post_process_slow`；`NO_INTERACTION` 挂 `hidden_state_decay` 12h tick + 去重 stamp；`body_memory` 经 `integrate_body_cue_and_save` 接线（仅 `can_write_memory=True` 时写入）；`hidden_state_debug` 新增 `trigger_counts`。见 `cc-tasks/88`，测试 `tests/test_hidden_state_reality_signals_brief88.py`，known-issues 已移入已关闭表 | 无，已关闭 |
| A2 | ~~**user_facts 全局事实库**~~ **已完成（Brief 89）** | `consolidate_to_identity` / `event_log_salvage` 两处固化点各加可选输出段 `global_facts`（ALLOWED key 白名单，每次 ≤3 条，坏 JSON 不影响主产物），统一走 `update_user_facts`（DENIED 名单拒绝 + `rejected_keys` 告警 + provenance_log）；DESIGN.md 决策 2「唯一写者」列已更新为「admin 手写 + consolidate_to_identity/event_log_salvage 分流」。见 `cc-tasks/89`，测试 `tests/test_global_facts_route.py` | 无，已关闭 |
| A3 | `core/paths.py` | taxonomy 命名空间占位模块，「当前不接 loader」 | 下个删除 brief 一并裁决：接上或删掉 |
| A4 | memory 类工具 | 仅 tool_loop 开启时进入生成侧（tool_loop 默认关）。config 未开则是死路径 | 确认 config.tool_loop 实际状态；长期关则记为「随 tool_loop 生效」不算债 |
| A5 | `DREAM_LOCKED` | 预留 enum 未实现（MVP 边界，挽留只走 RP 叙事） | 保留，无行动 |

## B. Demo / 低完成度模式

| # | 项 | 现状 | 建议 |
|---|---|---|---|
| B1 | **Dream scenario 模式**（v0–v0.8） | 引擎/守卫/阶段机齐全，但剧本库只有 `prison_demo.yaml` 一个示例；afterglow/impression 回流全关（v0.x 有意） | 内容工作：剧本库扩充（写 2-3 个正式剧本）。回流开闸是设计决策，等玩出需求再说 |
| B2 | ~~**Dream mirror 模式**（v0.1）~~ **已收尾（Brief 90，v0.2）** | `distill_impression()` 产出 entry 加 `mode: "sandbox"\|"mirror"`（存量无字段按 sandbox 兼容）；`impression_loader` 加 Reality gate（mirror 条目不参与出梦强注 3 轮，仅 body_intimate/physical_closeness/emotion.deep 标签可召回且带框定前缀）；mirror afterglow 开闸，走同一条 WriteEnvelope 路径，数值影响面维持既有不变量（只碰 sensitivity.current/embodied_ease）；scenario 仍全跳（设计不是欠账）。见 `cc-tasks/90`，`docs/dream.md` 已改写为「Mirror v0.2 门控写回」，测试 `tests/test_dream_mirror_v0.py` | 无，已关闭 |
| B3 | Stage dream domain | fail-closed（dream_state/body tracker/afterglow 都绑单角色，未 per-character 化） | 大工程，等真想玩梦境群聊再立项；现状是正确的 fail-closed |
| B4 | 群聊 QQ/mobile 无 UI | v1 只有 desktop 有群聊界面，QQ/mobile 走轮询无 round 概念 | 接受为 v1 边界；mobile 群 UI 看 88 后使用热度 |
| B5 | 桌面协议 v1 | v0.1 冻结，v1 未排期 | 决策性冻结，不是债 |

## C. 带闸门待观察（刻意的，别误当烂尾）

- **62 习惯固化**：等 56 视觉 shadow 14 天观察 + 幻觉率 <10% + sensitive 漏判 =0（调整指南在 00b §三）。
- **63 受限购物**：等 57 台账观察期 + **用户显式拍板（不可代拍）**。
- **PB4**：Path B 删除倒计时 **2026-08-10**——到期无缺口记录就开删除 brief，别忘。
- **ACT-2**：流式路径反坍缩重试未覆盖（需「暂缓前 N token vs 软降级」独立设计）。
- **P3**：裁剪后 `layers_activated` 遥测失真（open，小修）。
- 52 混合仲裁 / 49 LLM 级整合：按各自观察期评估；46 的 memory_digest 注入问题已被 Brief 80 归并退役**解决**。

## D. 未竟系列（00b §七 存量 + 更新）

1. 成长二期：对话内读作品集工具、笔记注入 prompt、「重拾搁置爱好」叙事。
2. 成长三期：可执行 skill 自写（**先别碰**，等 SkillAudit 式闸门基建）。
3. 媒介类 MCP 接入（ComfyUI 优先），前置 61 门控已落地。
4. identity-2：identity 注入冷启动观察（新用户首个有效维度轮数）。
5. DREAM-1：身份稳定性 eval 仍是弱代理，双轨观察中。

## E. 文档对齐小账（顺手修）——**2026-07-18 已全部核实/完成**

1. ~~00b §七说「55 valence 影响 arbiter 明确暂缓」~~ **已核实**：`cc-tasks/00b` §七第 96 行已直接写入 supersede 记录——「已被 Brief 85 §5 实现覆盖」（2026-07-17，±20% eagerness 微调版，见 DESIGN.md 决策 9.4）。无需再动。
2. ~~`docs/system-readiness.md` 停在老纪元~~ **已核实**：文件头部已加快照声明（「快照日期：2026-06 前后，仅存档参考」），并注明具体缺口清单以 known-issues 与本清单为准、去留留待下个删除 brief 裁决。无需再动。
3. ~~known-issues H1 条目引用的 `cc-tasks/08b` 文件已不存在~~ **已核实**：`docs/known-issues.md`「本轮已核对关闭」表 H1 行已随 Brief 88 落地同步，改指 `cc-tasks/88`（原 A1 工单），并移入已关闭表。无需再动。
4. `sleep_report` 已移除（scheduler 文档已标），无残留——仅历史引用（`docs/assistant-turn-sink.md`/`docs/scheduler.md` 均标注「未实现，已移除」），确认无代码残留。

## 建议的下一批顺序

~~A1（H1 接线，代拍保守映射）→ A2（user_facts 喂养）→ B2（mirror 收尾）→ E 三处顺手~~
**四项已于 2026-07-18 全部完成**（Brief 88/89/90 + 文档核实，`pytest -n auto` 93 passed）。

当前工作树里以上改动均已落地但**部分尚未提交**（`git status` 显示 core/dream、
core/memory/user_facts.py、docs/dream.md、docs/system-readiness.md、cc-tasks/00b
等为未跟踪/未暂存变更），建议下一步先确认是否分批 commit，再继续排期：

- B1（剧本内容，随缘）——唯一剩余、未拍板的常规项。
- A3/A4/A5：无行动或等外部裁决（随下个删除 brief / config 现状确认）。
- 62/63 严格等闸门数据，不排。
- 00b §一表末行「H1 接线工单：等用户拍板信号映射」现已过期（H1 已接线），
  下次碰 00b 时顺手勾掉。
