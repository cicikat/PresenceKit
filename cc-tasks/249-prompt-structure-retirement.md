# Brief 249 — Prompt 结构减法与失效规则清理

状态：完成；依赖 Brief 248。

## 检查表

- [x] 根据审计确定可删除的重复/失效规则，明确必须保留的语义边界。
- [x] 收拢框架规则归属；保留 `_layer`、独立裁剪、消融与来源，不合并真实消息 role。
- [x] 对已证实的 prompt 断链做最小修复或明确登记后续工单；不误删外部调用入口。
- [x] 同步删除旧守卫/僵尸测试/过期文档，补原路径和相邻路径回归。
- [x] 验证管理面与客户端既有控制面，未验证的真实模型效果标为 observe。
- [x] 运行相关测试与差异检查，勾选完成项并独立提交。

验收：明确列出删除内容、保留原因、修复证据与未完成范围。离线测试不得声称完成真实模型文风 A/B。

删除：未使用 LayerSpec、无生产调用的工具名单 helper 及三个专用测试；整轮静态工具禁令和过期说明。保留：真实 probe/schema、工具事实闸、历史裁剪/消融、记忆/时间的位置强化。

修复：mood 与无槽位 perception 注入、thinking scoped mood 名称、Path C 后续结果与静态指令冲突。
验证：首轮 212 passed，新增循环测试因 fixture 未使用 normalized tool-call 格式失败；修正后 61 passed；收尾 mock 调整后的循环四项复测全部通过。未运行全量/真实模型，不修改运行中的生产数据。

未完成：全步工具请求观测（open）、Dream/心声专项视角统一（roadmap）、真实模型文风 A/B 与真机（observe），已同步 known-issues 和三仓总账。管理面复用既有观测与消融，无静态资产变化，无新状态/接口/权限。
