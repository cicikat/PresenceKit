# 工单 242：管理面板与桌面偏好重整——交接及完成记录

日期：2026-09-10。状态：**已恢复施工，代码与浏览器夹具验收完成；实机边界为 observe，见文末**。

下文一至六节保留暂停时的工程状态；用户现已授权恢复施工，最终结果见第七节。两个仓库的未提交代码均保留，不回滚、不清理。本工单优先于初始审计报告中“尚未施工”的时间点描述。

## 一、已确认的目标

1. 桌面客户端偏好只承载界面、桌宠、连接、当前角色切换等便利设置；模型绑定、方案、思考、后端能力和权限、对话风格统一迁往管理面板。本机执行同意、截图许可、播放控制和停止操作仍需保留。
2. “一起做事”的偏好合并到主要聊天页偏好，不打断已有阅读或棋局。
3. 后端一级导航收敛为概览、功能与行为、服务配置、创作、观测、运维与调试。功能总开关统一呈现并跳转细分开关；服务配置首页只显示配置情况与缺项并跳转填写页。
4. 角色卡、现实设定、梦境设定归创作；模型绑定归配置。角色模型绑定支持“重置为跟随全局”，并显示实际生效模型。
5. 桌面“角色与对话”展示只读当前状态：当前角色、模型方案、预设、模型和配置情况；切换角色自动刷新。
6. 观测按用户排查任务归并，避免启用、配置、运行记录混杂；中文文案不再出现生硬中英拼接。

## 二、已完成并提交

| 提交 | 内容 | 验证 |
| --- | --- | --- |
| `c3ab4bf` | 初始设置与观测审计，见 `docs/settings-reorganization-audit-2026-09-10.md` | 源码审计；报告保留当时证据边界 |
| `1cdae9c` | 角色工具循环有效状态遵循实际解析；模型路由只读投影增加实际模型、全局方案、绑定来源和配置完整性 | 相关测试 5 项通过 |

第二项涉及 `core/control_center/effective_state.py` 与 `core/model_registry.py`。新增投影字段为 `resolved_chat_model`、`global_profile`、`binding_source`、`chat_configured`；沿用既有空值重置语义，没有新增配置存储。已同步后端控制面文档和三仓接口总账。

## 三、已实现但未提交、未完整验收

### 后端

- 六类侧栏与二级页面已接线，旧片段仍保留供深链接访问。
- 新增 `admin/static/js/settings-center.js`：总开关、配置摘要、角色绑定及重置、对话设置、统一记录入口。
- 新增页面：`feature-center`、`service-center`、`creation-center`、`observation-center`、`operations-center`、`role-bindings`、`conversation-settings`、`generation-debug`、`embedding-config`、`mail-config`、`diary-config`、`coplay-config`、`personal-settings`、`network-config`、`device-policy`、`output-settings`、`autonomy-settings`、`call-records`。
- 角色创作页旧模型/资源绑定编辑器已移走，改为入口；setup、runtime-config、提示词观测、自主行为观测中的部分编辑控件已迁到细分页。
- 相关 JS、片段及直接资源版本已调整为本轮版本；继续改动仍需更新缓存版本。
- 部分静态导航与表单守卫测试已更新，尚未完成最终回归。

### 桌面仓库

- `PreferencesPanel.tsx` 已移除模型和后端能力分类入口；角色分类加入 `CurrentCharacterStatus.tsx`。
- `ActivityAppearanceSettings.tsx` 合并原活动外观偏好，沿用原五个存储键。
- Activity 设置按钮通过 `open-activity-preferences` 打开主聊天偏好；Activity 是同一 WebView 的覆盖层，聊天仍挂载，设计上无需退出当前活动。
- 偏好信息架构、相关测试和中英文资源已调整；本机视觉同意、桌面播放偏好仍有入口。
- `src/shared/api/types.ts` 补模型状态字段。
- **暂停前最新变更**：`src/shared/api/backend.ts` 保留模型状态归一化字段，并给 prompt-assets 缓存加入代次防护，防止旧请求覆盖角色切换后的状态。此变更尚未测试。

## 四、恢复施工后的任务顺序

### A. 先修正确性与功能遗失

- [ ] 为桌面 `backend.test.ts` 补字段归一化测试，以及旧请求晚返回、缓存失效、角色切换的竞态回归；重新跑类型检查。
- [ ] `CurrentCharacterStatus` 遇到缺失 `model_routing` 时显示未知/未返回，不能误报“跟随全局”；验证切换、失败、手动刷新与窗口聚焦刷新。
- [ ] 修正 `centerSwitch` 细分页映射：目前除工具和工具循环外大多落入 `runtime-config`，调度、自主活动、语音等需要各自真实入口；记录入口也应对应功能。
- [ ] 核对 effective-state 的 `edit_page`，避免指向已搬空的旧栏目。
- [ ] 核对服务摘要真实响应结构；邮件、日记、OCR、外部工具、代理等必须准确区分未配置、未知、未测试，不能只显示泛化跳转提示。
- [ ] 核对 `restart_required` 返回类型，准确显示重启提示。
- [ ] 角色资源保存加忙碌和角色切换保护；纯文本角色不支持持久绑定时明确只读/不支持，不能等保存时报错。
- [ ] 检查模型配置完整性判断：无密钥本地模型是否被误报未配置，应与实际 provider 约定一致。
- [ ] 补查从桌面撤下的提示词资产启用组合、头像上传等是否在管理面仍有完整可操作入口；不得只删客户端入口而丢功能。
- [ ] 恢复已从自主行为观测移除的“手动测试入队”操作，放入运维/调试；评估自主提示词快照是否也应移入调试。
- [ ] 个人设置页填充用户选项后补调用 `loadUserPronoun()`，确认实际加载已有值。

### B. 完成分类与减法

- [ ] 总开关提供搜索或合理分组、统一行样式、简洁功能名及阻断说明，避免大量长描述再次堆成列表。
- [ ] 完成用户关心的中文清理；新页仍有硬编码中文，旧细分页仍有混合英文，按项目现有国际化方式收口。
- [ ] 检查统一观测是否可按问题定位；当前统一记录页主要是 JSON 详情，不能以有接口代替可用性验收。
- [ ] 删除无引用的桌面旧模型/能力偏好组件、独立 Activity 偏好组件及仅服务它们的常量和测试；先搜索引用，保留其他消费者使用的 API。
- [ ] 删除本次一次性迁移脚本：`scripts/reorganize_admin_settings.py`、`reorganize_desktop_preferences.py`、`split_settings_details.py`、`finish_settings_split.py`。它们当前未跟踪，不是交付物。
- [ ] 迁移相应守卫断言，特别是 setup、MCP 调试、自主行为表单及路由覆盖中已过时的“桌面独占”说明；不要放宽测试掩盖遗漏。

### C. 验证、文档、提交

- [ ] 跑相关后端 UI/API 测试，修复本次迁移造成的失败；检查模型页 input 的 type、OCR 控件 inline style，以及确实无引用的 CSS 类。先区分基线问题，不顺手处理无关改动。
- [ ] 跑桌面相关 Vitest、TypeScript 和 production build；最新缓存修改后必须重跑。
- [ ] 管理面硬刷新后实际打开受影响页，核对所有新入口、加载失败提示、开关保存和绑定重置；继续更新 fragment 与资源版本。
- [ ] 桌面验证角色切换后的模型信息、旧请求竞态，以及在活动中打开统一偏好不退出阅读/棋局。浏览器夹具与真实 Tauri 测试分别记录。
- [ ] 同步后端 `docs/feature-control-surface.md`、`docs/three-repo-interface-catalog.md`、审计报告；桌面 `docs/settings-control-audit.md` 及相关结构/接口文档。
- [ ] 未覆盖的手机/Tauri/调用链检查同时登记后端 `docs/known-issues.md` 和接口总账，明确 open/roadmap/observe，不宣称完成。
- [ ] 各工单相关测试通过、差异检查后独立提交；后端与桌面分别提交，只纳入本任务文件。

## 五、已做验证与边界

- 已提交核心改动：相关测试 5 项通过。
- 后端另一轮针对性测试曾为 27 项中 21 通过、6 失败；部分守卫随后更新，**没有最终全绿结果**。不能把该轮结果当作当前工作树结果。
- 桌面 TypeScript 与生产构建曾通过，但在最后的 `backend.ts` 缓存修改之前。
- 若干管理面 JS 语法检查通过。
- 管理面浏览器夹具验证曾实际打开 18 个新页面，无 pageerror；验证重置请求为 PATCH `/character/example/model-routing`，正文 `{model_routing:null}`，界面显示全局方案和模型；也验证思考设置保存请求。
- 浏览器测试加载真实静态资源，**API 全部使用夹具拦截**，且通过 CDP 清缓存并禁用缓存。这是页面交互验证，不是生产配置或真实后端保存验证。
- 截图与浏览器脚本位于忽略目录 `.tmp/settings-browser/`。此前内置浏览器工具不可用，采用本地 Playwright；未完成真实桌面 Tauri 验证。

## 六、恢复时的环境与保护事项

- 开始先读两个仓库 AGENTS.md 和后端 `docs/dev-environment.md`；重新检查 Git 状态，不能假设本工单之后无人修改。
- 后端本来就有 `cc-tasks/229` 至 `241` 的删除，以及 `cc-tasks/临时工单.md` 修改；桌面 `AGENTS.md` 也原有修改。均不属于本任务，不回滚、不提交。
- 两仓产品 UI 改动目前仍未提交。桌面暂停时 HEAD 为 `0c241c0`，后端为 `1cdae9c`。
- 测试用指定 Python 环境和针对性 `pytest -n auto`；禁止为方便改全局 Git 配置。注意源文件换行，避免无意义整文件变化。
- 此前为浏览器验证启动的本地 uvicorn 服务使用端口 `18081`，会话编号 `73663`、进程编号 `20880`；暂停时未核实其是否仍存活。恢复时先查命令行和端口归属，不能仅凭旧进程编号终止进程。
- 本轮按用户要求仅新增此交接工单，未继续修改产品代码、运行验收或提交未验收改动。

## 七、恢复施工后的完成记录

A/B 施工项已处理：缓存归一化与竞态回归、未知模型状态、真实设置/记录入口映射、
服务配置状态、boolean/list 重启提示、绑定写入忙碌和角色保护、纯文本卡只读、兼容模型
无密钥状态、创作资产启用组合和头像上传/恢复、个人称谓加载、运维测试入队与完整自主
提示词迁移。功能搜索与统一行样式、中英文新文案、可读调用记录及筛选已完成。
旧桌面偏好组件和独立 Activity 偏好已删除；一次性迁移脚本已删除；过时页面守卫已迁移。

最终验证：
- 后端针对性 UI/API 89 项通过（含模型绑定、资源绑定、prompt assets 和图片识别相邻路径）。
- 桌面相关 Vitest 40 项通过，TypeScript 和生产构建通过。
- 管理面浏览器：清缓存、禁用缓存后实际加载 18 个新页，检查中英文、搜索、重置绑定、
  思考与调度开关、资产组合保存；纯文本卡只读、资源保存也通过。无 pageerror。
- 桌面浏览器：实际挂载 React 组件，在阅读第 2 页时打开统一偏好，阅读节点未卸载、
  无 close 请求；角色切换、缺字段未知、聚焦刷新、失败后手动重试通过；头像裁剪位于偏好上层，未被遮挡。
- 浏览器载入真实资源，API/IPC 使用夹具，没有修改生产配置；截图/脚本在忽略的临时目录。
- 额外工具说明覆盖测试存在既有 `reread_image` 中英文文案缺失，未放宽测试或修改无关工具。

`observe`：未完成真实 Tauri 原生窗口、真实上游连通性及手机设备联调；本次没有修改
手机、WS、poll/ack/TTL 或 relay 协议。相关边界同步 known-issues 和三仓接口总账。
后端控制面、审计与接口文档及桌面结构/设置/接入文档均已同步。
两仓仅提交本任务文件，保留原有 cc-tasks 删除/临时工单及桌面 AGENTS.md 改动。
